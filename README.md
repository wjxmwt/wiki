# iGEM Wiki

这是一个使用Python Flask框架的简单Web应用项目。

## 安装依赖

请确保你的计算机上已安装 Python 3.8 或更高版本，然后执行以下命令安装运行所需的扩展包：

```bash
pip install -r requirements.txt
```

## 启动本地开发环境

在开发过程中，你可以启动支持热重载的 Flask 本地服务器：

```bash
python app.py
```

启动后在浏览器中访问：`http://127.0.0.1:5000/`